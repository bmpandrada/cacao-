

export default function OrderPage({
    children, // will be a page or nested layout
  }) {

    return (
      <section>
        <nav>
        
        </nav>
   
        {children}
      </section>
    )
  }